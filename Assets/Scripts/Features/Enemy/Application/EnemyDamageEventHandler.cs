namespace Features.Enemy.Application
{
    // Legacy shim kept so older scene/code references do not produce missing-file
    // project errors while damage publishing lives in EnemyCombatTargetProvider.
    public sealed class EnemyDamageEventHandler
    {
    }
}
