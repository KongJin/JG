namespace Features.Status.Application
{
    // Legacy shim kept to avoid transient project-file compile errors after the
    // status-application bridge was moved into producer features.
    public sealed class StatusTriggerHandler
    {
    }
}
